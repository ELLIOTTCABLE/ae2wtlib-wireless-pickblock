min_version = "2024.11.3"

# JDK is the only build dependency not fetched by Gradle itself.
# NeoForge / Minecraft 1.20.4 build on Java 17.
# Gradle comes from the ./gradlew wrapper; NeoForge, AE2, and AE2WTLib are resolved by Gradle.
[tools]
java = "temurin-17"
